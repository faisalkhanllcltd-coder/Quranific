---
// src/components/ui/EyebrowBadge.astro
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center justify-center gap-1.5 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.15em] rounded-full',
    className,
  ]}
>
  <slot />
</span>
