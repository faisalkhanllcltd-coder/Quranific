# 01 — Cloudflare Audit

**Audit Date:** 2026-09-21 | **Auditor:** Antigravity AI (Second Auditor)  
**Stack:** Cloudflare Workers (Static Assets via `wrangler.toml` `[assets]`) | `@astrojs/cloudflare` 14.2.0 | Wrangler 4.131.2  
**Platform confirmed:** Cloudflare **Workers** (custom domain routes + `[assets]` block in wrangler.toml — NOT Cloudflare Pages)

---

| ID | Practice | Why it matters (1 line) | Source URL | Label | Status | Evidence | How to fix | Effort | Impact | Risk |
|---|---|---|---|---|---|---|---|---|---|---|
| CF-01 | **compatibility_date freshness** | Stale date locks you out of newer runtime improvements and security fixes | https://developers.cloudflare.com/workers/configuration/compatibility-dates/ | [VERIFIED-DOC] | ✅ DONE | `wrangler.toml` L2: `compatibility_date = "2026-03-25"` — within 12 months of audit date 2026-09-21 | Bump to `2026-09-01` or later on next deploy | S | M | Low |
| CF-02 | **nodejs_compat flag present** | Required for Astro SSR adapter (`fs`, `path`, `process` used by Vite/Astro internals) | https://developers.cloudflare.com/workers/runtime-apis/nodejs/ | [VERIFIED-DOC] | ✅ DONE | `wrangler.toml` L3: `compatibility_flags = ["nodejs_compat"]` | — | — | — | — |
| CF-03 | **Smart Placement enabled** | Routes each invocation to the datacenter nearest the origin data source, reducing latency | https://developers.cloudflare.com/workers/configuration/smart-placement/ | [VERIFIED-DOC] | ✅ DONE | `wrangler.toml` L11–12: `[placement] mode = "smart"` | — | — | — | — |
| CF-04 | **Secrets vs Vars: sensitive values** | `[vars]` are plaintext in wrangler.toml and git; secrets must use `wrangler secret put` | https://developers.cloudflare.com/workers/configuration/secrets/ | [VERIFIED-DOC] | ⚠️ PARTIAL | `wrangler versions view` confirms `JWT_SECRET`, `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY` exist in Cloudflare. `META_PIXEL_ID` and `META_CAPI_TOKEN` are ABSENT. Note: Pixel ID is public client-side metadata and does not need to be a secret; `META_CAPI_TOKEN` MUST be a secret. `complete.ts:243-247` safely skips CAPI when unset. | Run `wrangler secret put META_CAPI_TOKEN` and add `META_PIXEL_ID` to `[vars]` or remove dead CAPI code | S | H | Low |
| CF-05 | **`wrangler types` generated** | Keeps `env` bindings type-safe; prevents runtime access mistakes | https://developers.cloudflare.com/workers/wrangler/commands/#types | [VERIFIED-DOC] | ❌ MISSING | `worker-configuration.d.ts` is absent from repo. `src/env.d.ts` contains manually typed `Env` interface prone to drift. | Run `npx wrangler types` to generate `worker-configuration.d.ts` and reference it in `src/env.d.ts` | S | M | Low |
| CF-06 | **KV consistency model awareness** | KV is eventually consistent; for critical idempotency use durable objects or read-your-writes pattern | https://developers.cloudflare.com/kv/api/ | [VERIFIED-DOC] | ⚠️ PARTIAL | KV used for idempotency (`complete.ts` L166–176) and rate limiting. Eventual consistency window means simultaneous cross-PoP requests can pass. Free plan write quota: 1,000 writes/day. | Acceptable for current low traffic; migrate rate limiting to Cloudflare WAF Rate Limiting rules | M | M | Low |
| CF-07 | **Observability / logs enabled** | Enables real-time log streaming and tail workers for debugging | https://developers.cloudflare.com/workers/observability/ | [VERIFIED-DOC] | ⚠️ PARTIAL | `wrangler.toml` L24: `[observability] enabled = false` (top-level) but `[observability.logs] enabled = true`. Workers Logs works independently per Cloudflare docs, but top-level `false` disables trace/metrics collection. | Set `[observability] enabled = true` in `wrangler.toml` to enable full dashboard observability | S | M | Low |
| CF-08 | **Static assets cache: hashed `/_astro/*` immutable** | Immutable cache for content-hashed assets is CDN best practice; eliminates unnecessary revalidation | https://developers.cloudflare.com/cache/concepts/cache-control/ | [VERIFIED-DOC] | ✅ DONE | `public/_headers` L14–15: `/_astro/* Cache-Control: public, max-age=31536000, immutable`. Measured live: `cache-control: public, max-age=31536000, immutable`, `cf-cache-status: HIT`. | — | — | — | — |
| CF-09 | **HTML caching strategy** | SSR HTML must be cacheable at edge but freshly validated by browsers | https://developers.cloudflare.com/cache/concepts/cache-control/ | [VERIFIED-DOC] | ⚠️ PARTIAL | Live curl to `/` returns NO `cf-cache-status` header; `Date` changes every hit. Cloudflare CDN does NOT edge-cache Worker-generated responses by default without Cache API or Cache Everything rule, despite `CDN-Cache-Control` header. | Implement Cloudflare Cache Rules (Cache Everything) for static/prerendered SSR routes or migrate routes to `prerender = true` | M | H | Low |
| CF-10 | **API routes not edge-cached** | POST endpoints must not be cached; leaks responses across users | https://developers.cloudflare.com/cache/concepts/default-cache-behavior/ | [VERIFIED-DOC] | ✅ DONE | `src/middleware.ts` L101: `!context.url.pathname.startsWith('/api/')` ensures caching headers only apply to HTML routes | — | — | — | — |
| CF-11 | **Security headers: HSTS** | Prevents protocol downgrade attacks | https://developers.cloudflare.com/ssl/edge-certificates/additional-options/http-strict-transport-security/ | [VERIFIED-DOC] | ⚠️ PARTIAL | On SSR (`/`): `Strict-Transport-Security: max-age=31536000; includeSubDomains` (missing `preload`). On static (`/courses/`): `preload` present via `_headers`. Disparity between Worker and Static Assets. Subdomains must be verified before preload submission. | Add `; preload` to `src/middleware.ts` L10 to match `_headers`. Verify all subdomains before hstspreload.org submission. | S | M | Low |
| CF-12 | **Security headers: COOP/COEP** | Cross-Origin Opener Policy prevents cross-origin window attacks (Spectre mitigation) | https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cross-Origin-Opener-Policy | [VERIFIED-DOC] | ❌ MISSING | Measured live on all routes: `Cross-Origin-Opener-Policy` and `Cross-Origin-Embedder-Policy` headers are completely absent | Add `'Cross-Origin-Opener-Policy': 'same-origin-allow-popups'` in `src/middleware.ts` and `_headers` (prevents breaking WhatsApp/auth popups) | S | H | Low |
| CF-13 | **CSP: `unsafe-eval` absent on live site** | `unsafe-eval` enables XSS; must not appear in CSP | https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP | [VERIFIED-DOC] | ⚠️ PARTIAL | Live SSR (`/`) CSP has NO `unsafe-eval`. BUT live prerendered static assets (`/courses/`) served from `_headers` DO contain `unsafe-eval` in CSP! GTM custom JS or Partytown may require evaluation; test via Report-Only CSP first. | Remove `unsafe-eval` from `public/_headers` L7 after verifying in Report-Only mode | S | H | Medium |
| CF-14 | **CSP: `unsafe-inline` in `script-src`** | `unsafe-inline` in `script-src` negates CSP protection entirely for XSS | https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP | [VERIFIED-DOC] | ⚠️ PARTIAL | `src/middleware.ts` L13 and `public/_headers` L7 use `'unsafe-inline'` for Partytown and inline scripts. Nonces conflict with prerendered/cached HTML. | Transition to SHA-256 script hashes for static inline scripts and Partytown snippet | M | H | Medium |
| CF-15 | **Turnstile on form endpoints** | Bot abuse protection on server-side API routes | https://developers.cloudflare.com/turnstile/get-started/server-side-validation/ | [VERIFIED-DOC] | ⚠️ PARTIAL | Present on `/api/register`, `/api/contact`, `/api/apply-teacher`. ABSENT on `/api/newsletter` (unprotected against automated subscription spam). | Add Turnstile challenge to newsletter form and server verification in `/api/newsletter.ts` | S | H | Low |
| CF-16 | **Rate limiting via KV** | Prevents enumeration and spam abuse on API endpoints | https://developers.cloudflare.com/kv/api/ | [VERIFIED-DOC] | ⚠️ PARTIAL | KV rate limiting (4 req/60s) implemented on all 5 POST endpoints. However KV is eventually consistent, fails open on error (`register.ts:72`, `contact.ts:75`, `apply-teacher.ts:85`, `newsletter.ts:41`), and risks 1,000 write/day quota on Free plan. | Configure Cloudflare WAF Rate Limiting rules at edge (pre-Worker) for primary defense | M | H | Low |
| CF-17 | **www → apex redirect** | Canonical URL hygiene, SEO duplicate content prevention | https://developers.cloudflare.com/rules/url-forwarding/ | [VERIFIED-DOC] | ✅ DONE | Measured live: `curl.exe -sI https://www.quranific.com/` returns HTTP 301 redirect to `https://quranific.com/` | — | — | — | — |
| CF-18 | **HTTP/3 (h3) advertised** | Reduces connection establishment latency, especially on mobile | https://developers.cloudflare.com/speed/optimization/protocol/http3/ | [VERIFIED-DOC] | ✅ DONE | Measured live: `alt-svc: h3=":443"; ma=86400` present on all live responses | — | — | — | — |
| CF-19 | **Cloudflare Speculation Rules** | Cloudflare injects its own speculation rules (per `Speculation-Rules: "/cdn-cgi/speculation"` header) | https://developers.cloudflare.com/speed/optimization/content/speculation-rules/ | [VERIFIED-DOC] | ✅ DONE | Measured live: `Speculation-Rules: "/cdn-cgi/speculation"` header present on live site. Compatible with site rules. | Review site speculation rules to ensure no duplicate prefetching of SSR routes | S | L | Low |
| CF-20 | **Partytown + GTM: CSP `frame-src` allows GTM** | GTM iframe (noscript) requires `frame-src: googletagmanager.com` | https://developers.cloudflare.com/workers/configuration/csp/ | [VERIFIED-DOC] | ✅ DONE | `src/middleware.ts` L18: `frame-src 'self' https://challenges.cloudflare.com https://www.googletagmanager.com` | — | — | — | — |
| CF-21 | **Partytown breaking CSP: `worker-src blob:`** | Partytown uses a blob: worker for GTM off-main-thread execution | https://partytown.builder.io/proxying-requests | [VERIFIED-DOC] | ✅ DONE | `src/middleware.ts` L19: `worker-src 'self' blob:` | — | — | — | — |
| CF-22 | **`*.workers.dev` preview indexation** | Cloudflare auto-creates `<name>.workers.dev` which may be publicly reachable | https://developers.cloudflare.com/workers/configuration/routing/routes/ | [VERIFIED-DOC] | ⚠️ PARTIAL | `wrangler.toml` root binds `quranific.com` and `www.quranific.com` but lacks explicit `workers_dev = false`. `alarm-worker/wrangler.toml` defines NO routes, defaulting to `workers_dev = true` on Wrangler v3/v4; its HTTP handlers (`/force-run`, `/resend-log`) are publicly reachable without auth. | Add `workers_dev = false` to both `wrangler.toml` and `alarm-worker/wrangler.toml` | S | H | Low |
| CF-23 | **`prerender = false` on API routes** | Prevents build-time static generation of server-only endpoints | https://docs.astro.build/en/guides/on-demand-rendering/ | [VERIFIED-DOC] | ✅ DONE | All API routes under `src/pages/api/` explicitly declare `export const prerender = false;` | — | — | — | — |
| CF-24 | **No `run_worker_first` in `[assets]`** | Modern Cloudflare Workers Static Assets no longer needs this; its removal improves static performance | https://developers.cloudflare.com/workers/static-assets/ | [VERIFIED-DOC] | ✅ DONE | `wrangler.toml` L7–8 confirms `run_worker_first` was intentionally omitted; static assets served direct from edge CDN | Note: Because worker does not run first, middleware headers do NOT apply to static assets (handled by `_headers`) | — | — | — |
| CF-25 | **Dead-letter queue using KV** | Prevents lead loss when Resend API fails | https://developers.cloudflare.com/kv/api/ | [VERIFIED-DOC] | ✅ DONE | API routes write failed email payloads to KV with 30-day TTL (`expirationTtl: 2592000`) | Implement dead-letter monitoring/replay alert via `alarm-worker` | M | H | Low |
| CF-26 | **`waitUntil` pattern for background tasks** | Prevents response from closing before background tasks complete in Workers | https://developers.cloudflare.com/workers/runtime-apis/context/#waituntil | [VERIFIED-DOC] | ✅ DONE | In `@astrojs/cloudflare` 14.2.0, `locals.cfContext.waitUntil` IS the official API (`cf-helpers.js` L14). Code correctly calls `locals.cfContext?.waitUntil(...)` with async fallback. | — | — | — | — |
| CF-27 | **Cloudflare WAF managed rules** | Automatic protection against OWASP Top 10 and known attack patterns | https://developers.cloudflare.com/waf/managed-rules/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Requires Cloudflare dashboard or API token to verify active rulesets | Verify Cloudflare Managed Ruleset is enabled in Cloudflare Dashboard -> Security -> WAF | S | H | Low |
| CF-28 | **Bot Fight Mode** | Reduces automated scraping and form spam at the network edge | https://developers.cloudflare.com/bots/get-started/free/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Requires Cloudflare dashboard or API token to verify | Verify Bot Fight Mode is enabled in Dashboard -> Security -> Bots | S | H | Low |
| CF-29 | **Account 2FA** | Prevents account takeover; if account is compromised, entire site is | https://developers.cloudflare.com/fundamentals/setup/account/account-security/2fa/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Requires Cloudflare dashboard account settings check | Ensure 2FA is enforced on all account members via Cloudflare Dashboard -> My Profile -> Authentication | S | H | None |
| CF-30 | **Scoped API tokens (not Global API Key)** | Global API Key grants full account access; scoped tokens limit blast radius | https://developers.cloudflare.com/fundamentals/api/get-started/create-token/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Requires Cloudflare dashboard API Tokens check | Ensure CI/Wrangler deployments use tokens scoped only to Workers Scripts:Edit and Account:Read | S | H | Low |
| CF-31 | **Email DNS: SPF for quranific.com** | Prevents email spoofing from the domain | https://resend.com/docs/dashboard/domains/introduction | [VERIFIED-DOC] | ✅ DONE | Measured live via DNS TXT query: `v=spf1 include:_spf.mail.hostinger.com include:resend.com ~all`. Root SPF already includes Resend and Hostinger! | Do NOT overwrite root SPF (draft's recommendation would break Hostinger mail) | — | — | — |
| CF-32 | **Email DNS: DKIM for Resend** | Authenticated email delivery; prevents rejection as spam | https://resend.com/docs/dashboard/domains/introduction | [VERIFIED-DOC] | ✅ DONE | Measured live via DNS CNAME query: `resend._domainkey.quranific.com` resolves to `dkim.resend.com` | — | — | — | — |
| CF-33 | **Email DNS: DMARC policy** | Tells receiving mail servers what to do with emails that fail SPF/DKIM | https://resend.com/docs/dashboard/domains/introduction | [VERIFIED-DOC] | ⚠️ PARTIAL | Measured live via DNS TXT query: `_dmarc.quranific.com` has `v=DMARC1; p=none`. Active in monitoring mode (RFC 7489 recommended initial phase). | Create `compliance@quranific.com` mailbox, add `rua=mailto:compliance@quranific.com`, monitor reports, then advance to `p=quarantine` | S | H | Low |
| CF-34 | **Custom 404 page** | Branded error pages improve UX; Cloudflare serves its own 404 if no custom page exists | https://developers.cloudflare.com/workers/static-assets/routing/#not-found-behavior | [VERIFIED-DOC] | ✅ DONE | `src/pages/404.astro` and `src/pages/500.astro` present and rendered | — | — | — | — |
| CF-35 | **Rocket Loader status** | Rocket Loader defers JS which can break Partytown's synchronous worker bootstrap | https://developers.cloudflare.com/speed/optimization/content/rocket-loader/ | [VERIFIED-DOC] | ✅ DONE | Measured live: Inspection of raw HTML of home (`https://quranific.com/`) yielded: `rocket-loader`: 0 matches, `data-cf-`: 1 match (Web Analytics beacon), `beacon`: 2 matches, `email-decode`: 0 matches. Rocket Loader is disabled. | Keep Rocket Loader disabled in Cloudflare Speed -> Optimization | — | — | — |
| CF-36 | **Auto Minify status** | Cloudflare's auto-minify is deprecated; rely on Vite/Astro build-time minification | https://developers.cloudflare.com/speed/optimization/content/troubleshooting/disable-auto-minify/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Cloudflare deprecated Auto Minify on 2024-08-05. Zone status requires `GET /zones/{zone_id}/settings/minify` API check with token. "HTML looks minified" is not valid CDN evidence; Astro build minifier (`compressHTML: true`) minifies at build time. | Query Cloudflare API `GET /zones/{zone_id}/settings/minify` to verify all minification values (`html`, `css`, `js`) are `"off"` | S | L | Low |
| CF-37 | **CI: `npm audit --audit-level=critical || true` & Deploy Guard** | `|| true` swallows audit failures; ungated manual CLI deploys bypass CI checks | https://docs.npmjs.com/cli/commands/npm-audit | [VERIFIED-DOC] | ❌ MISSING | `.github/workflows/ci.yml` L29: `npm audit --audit-level=critical \|\| true` swallows audit exit codes entirely. Furthermore, live deployments are performed manually via `wrangler deploy` by developer without CI gate. Note: `npm audit --omit=dev --audit-level=high` currently fails due to `astro <7.2.8` and `sharp <0.35.4`; framework upgrades must precede CI gating or use an allowlist. | Put astro ≥7.2.8 and sharp ≥0.35.4 in Batch 1 before removing `\|\| true`; add deploy guard script requiring clean tree and passing checks before `wrangler deploy` | S | H | Low |
| CF-38 | **CI: Production branch protection** | Prevents direct pushes to `main` that bypass CI | https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches | [VERIFIED-DOC] | ❓ CANNOT VERIFY | `gh` CLI not installed in environment; requires manual GitHub repository settings verification | Enforce branch protection on `main`: require PR review and CI status checks before merge | S | H | Low |
| CF-39 | **Logpush for production logs** | `tail` is ephemeral; Logpush persists logs to R2/S3/Splunk for audit trails | https://developers.cloudflare.com/workers/observability/logs/logpush/ | [VERIFIED-DOC] | ➖ N/A | If Free plan: Workers Logpush is unavailable. If Paid plan ($5/mo): Logpush is available. Owner Cloudflare plan is UNKNOWN. | If Paid: configure Logpush to R2 bucket for audit retention. If Free: rely on real-time Workers Logs in dashboard. | M | M | Low |
| CF-40 | **`Pragma: no-cache` on complete.astro** | `Pragma` is deprecated HTTP/1.0 header; not needed alongside `Cache-Control: no-store` | https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Pragma | [VERIFIED-DOC] | ⚠️ PARTIAL | `src/pages/getting-started/complete.astro` L13 sets legacy `Pragma: no-cache` and L14 `Expires: 0` alongside `Cache-Control: no-store` | Clean up `complete.astro` by removing `Pragma` and `Expires` headers | S | L | Low |
| CF-41 | **DNSSEC validation & signing** | Protects DNS responses from spoofing and cache poisoning | https://developers.cloudflare.com/dns/dnssec/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | RDAP lookup (`GET https://rdap.verisign.com/com/v1/domain/quranific.com`) confirms registrar is `HOSTINGER operations, UAB` with `SecureDNS: { delegationSigned: false }`. Cloudflare DoH query confirms no DS record in .com parent zone. Requires owner to enable DNSSEC in Cloudflare dashboard and paste DS record into Hostinger registrar console. | Enable One-Click DNSSEC in Cloudflare DNS -> Settings, copy DS record (Key Tag, Algorithm, Digest Type, Digest), and add to Hostinger domain DNSSEC settings | S | H | Low |
| CF-42 | **CAA DNS records** | Restricts which Certificate Authorities can issue SSL certificates for the domain | https://developers.cloudflare.com/ssl/edge-certificates/caa-records/ | [VERIFIED-DOC] | ❌ MISSING | Measured live via DoH: No CAA record set on `quranific.com`. Misconfigured CAA can prevent Universal SSL certificate auto-renewal, causing site downtime. Exact Universal SSL CAs: `letsencrypt.org`, `digicert.com`, `sectigo.com`, `pki.goog`. | Add CAA records in Cloudflare DNS: `0 issue "letsencrypt.org"`, `0 issue "digicert.com"`, `0 issue "sectigo.com"`, `0 issue "pki.goog"`, `0 issuewild ";"` | S | H | High |
| CF-43 | **SSL/TLS encryption mode (Full Strict)** | Prevents MITM attacks between Cloudflare edge and custom origin servers | https://developers.cloudflare.com/ssl/origin-configuration/ssl-modes/full-strict/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Platform uses Workers Static Assets (no external origin server), but if any custom origin route exists, Full (strict) is mandatory | Verify SSL/TLS mode is set to 'Full (strict)' in Cloudflare Dashboard -> SSL/TLS | S | H | Low |
| CF-44 | **Minimum TLS version 1.2 / TLS 1.3** | Deprecates vulnerable TLS 1.0 and 1.1 ciphers | https://developers.cloudflare.com/ssl/edge-certificates/additional-options/minimum-tls/ | [VERIFIED-DOC] | ✅ DONE | Measured live: Connection establishes over TLS 1.3 (`alt-svc: h3=":443"` advertised) | Confirm Minimum TLS Version is set to 1.2 in Cloudflare Dashboard -> SSL/TLS -> Edge Certificates | S | M | Low |
| CF-45 | **Always Use HTTPS** | Automatically redirects insecure HTTP traffic to HTTPS at edge | https://developers.cloudflare.com/ssl/edge-certificates/additional-options/always-use-https/ | [VERIFIED-DOC] | ✅ DONE | Measured live: `curl.exe -sI http://quranific.com/` returns HTTP 301 redirect to `https://quranific.com/` with HSTS | — | — | — | — |
| CF-46 | **Early Hints (103)** | Sends 103 status code with Link headers before main response to preload critical fonts | https://developers.cloudflare.com/speed/optimization/content/early-hints/ | [VERIFIED-DOC] | ❌ MISSING | Measured live: No 103 Early Hints returned on initial request to `/` or `/courses/` | Enable Early Hints in Cloudflare Speed -> Optimization -> Content Optimization | S | M | Low |
| CF-47 | **Workers plan execution limits** | Free plan limits: 10ms CPU time, 100k req/day. Paid plan: 50ms (or 30s) CPU time. | https://developers.cloudflare.com/workers/platform/limits/ | [VERIFIED-DOC] | ⚠️ PARTIAL | Owner plan is UNKNOWN. If Free plan: 10ms CPU limit risks Worker exceptions during complex SSR; prerendering static routes mitigates risk. If Paid plan: CPU limits are generous (up to 30s Wall time / 50ms standard). | Prerender all static marketing pages (`prerender = true`) to minimize edge CPU usage regardless of plan | M | H | Low |
| CF-48 | **Alarm-Worker HTTP endpoint exposure & Secret Reuse** | Background worker must not be open to public abuse or reuse JWT signing secret | https://developers.cloudflare.com/workers/runtime-apis/handlers/fetch/ | [VERIFIED-CODE] | ❌ MISSING | `alarm-worker/src/index.ts` L33–80 exposes `/force-run` and `/resend-log` with NO incoming authorization check. Furthermore, L46 forwards `Authorization: Bearer ${env.JWT_SECRET}`, creating dangerous secret reuse (cookie signing secret used as API auth token). `/api/internal/retry-queue.ts` L67 validates using non-constant-time equality (`!==`). | Add incoming Bearer auth on `alarm-worker`, migrate internal API auth to dedicated `INTERNAL_API_TOKEN`, and compare using `crypto.subtle.timingSafeEqual` | S | H | High |
| CF-49 | **Cloudflare Web Analytics beacon** | Edge-based privacy-first analytics | https://developers.cloudflare.com/analytics/web-analytics/ | [VERIFIED-DOC] | ✅ DONE | Measured live: Inspection of raw HTML confirms Cloudflare Web Analytics beacon is active (`static.cloudflareinsights.com/beacon.min.js`, token `0589387b238e41af8aafea4d88cd2c64`, `spa: 2`) injected via `src/layouts/Base.astro` L245–248 | — | — | — | — |
| CF-50 | **Page Shield & Hotlink Protection** | Monitors client-side script dependencies and prevents external image bandwidth theft | https://developers.cloudflare.com/page-shield/ | [VERIFIED-DOC] | ❓ CANNOT VERIFY | Requires dashboard check. Page Shield is available on Free/Paid plans; Hotlink Protection is under Scrape Shield. | Enable Scrape Shield -> Hotlink Protection in dashboard; configure Page Shield to monitor script changes | S | M | Low |
