---
// src/components/ui/EyebrowBadge.astro
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center justify-center gap-1.5 px-3 py-1.5 eyebrow-pill rounded-full',
    className,
  ]}
>
  <slot />
</span>
